@model IEnumerable<AppliVol.Models.Ville>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdVille)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nom)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdVille)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nom)
            </td>
            <td>
                    @Html.ActionLink("Edit", "Edit", new {  id=item.IdVille  }) |
                    @Html.ActionLink("Details", "Details", new {  id=item.IdVille  }) |
                @Html.ActionLink("Delete", "Delete", new {  id=item.IdVille  })
            </td>
        </tr>
}
    </tbody>
</table>
